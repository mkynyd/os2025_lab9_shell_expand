# ========== 忽略 build 输出 / 中间文件 /二进制 /可执行文件 ==========
/build/                # 忽略 build 目录及其所有内容
build/                 # 忽略 build 目录（如果在项目根目录外部生成）
*.o                    # 忽略所有 .o 对象文件
*.obj                  # 忽略 .obj 文件 (如果有的话)
/*.elf                 # 忽略根目录下 .elf 可执行／镜像文件
/*.bin                 # 忽略根目录下 .bin 文件
*.out                  # 忽略 .out 文件
*.map                  # 链接 map 文件
*.sym                  # 符号文件
*.lst                  # 列表 /汇编输出
*.d                    # 自动生成的依赖 /makefile 依赖文件
*.tmp                  # 临时文件
*.log                  # 日志文件
*.img                  # 镜像文件 (如果有的话)

# ========== 忽略 IDE / 编辑器 /系统 /临时 /缓存 文件 ==========
# VS Code
.vscode/
# 如果你用其它编辑器 /IDE，可加类似配置，例如 .idea/ (CLion / IntelliJ)：
.idea/

# 临时 /备份 /系统自动生成文件
*~                     # 例如 “file.c~” 之类的编辑器备份
*.swp                  # vim /编辑器 swap 文件
.DS_Store              # macOS 的文件系统元数据文件
Thumbs.db              # Windows 缩略图缓存
*.tmp                  # 临时 /缓存
*.bak                  # 备份文件
*.cache                # 缓存目录 /文件

# ========== 忽略 shell 脚本执行生成 /本地运行产生的文件 /临时输出 ==========
*.o.cmd                # 有些构建系统可能生成命令脚本输出
*.cmd                  # 临时脚本 /命令脚本
*_build_logs/          # 假设有 build 日志目录
build_logs/            # 与上类似
*.log                  # 通用日志 (已覆盖)

# ========== 忽略由工具 /构建系统 /包管理生成的外部库 /下载 /缓存 ==========
/third_party/          # (如果你的项目有 third_party / external / libs 文件夹)
/external_libs/        # 外部库
*.a                    # 静态库文件 (如果不希望版本控制)
/lib/                  # 库目录 (如果编译输出 /库文件在这里)

/# ========== 忽略用户 /环境 /本地配置 /敏感 /凭据 /私有文件 ==========
.env                   # 环境变量 /配置文件
*.key                  # 私钥 /密钥
*.crt                  # 证书文件
*.pem                  # 私钥 /证书文件
.idea/                 # IDE 配置 (已列出)
/.vscode/              # 编辑器配置 (已列出)

/# ========== 保留 /允许跟踪 ========== 
# src/、include/、kernel/、bsp/、脚本 (.sh, .c, .ld, CMakeLists.txt 等) —— 都是源码 /必要文件，不应忽略。
